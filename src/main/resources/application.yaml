spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stripe
    username: postgres
    password: asdf1234
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG